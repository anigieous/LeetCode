class Solution {
public:
    vector<int> getRow(int k) {
        long long n=1;
    vector<int> ans;
        for(int i=0;i<=k;i++){
            ans.push_back(n);
            n=n*(k-i)/(i+1);    //maths
        }
        return ans;
    }
};
